# {{ .Name }}

{{ template "chart.versionBadge" . }}{{ template "chart.typeBadge" . }}{{ template "chart.appVersionBadge" . }}

## Install

```bash
helm upgrade {{ .Name }} oci://quay.io/kubermatic/helm-charts/{{ .Name }} --version {{ template "chart.version" . }} --namespace kubelb --create-namespace --install
```

## Example with Ingress

```bash
helm install {{ .Name }} oci://quay.io/kubermatic/helm-charts/{{ .Name }} \
  --set ingress.enabled=true \
  --set ingress.className=nginx \
  --set ingress.hosts[0].host=app.example.com \
  --set ingress.hosts[0].paths[0].path=/ \
  --set ingress.hosts[0].paths[0].pathType=Prefix
```

## Example with HTTPRoute (Gateway API)

```bash
helm install {{ .Name }} oci://quay.io/kubermatic/helm-charts/{{ .Name }} \
  --set httpRoute.enabled=true \
  --set httpRoute.parentRefs[0].name=my-gateway \
  --set httpRoute.parentRefs[0].namespace=gateway-system \
  --set httpRoute.hostnames[0]=app.example.com
```

Requires the Gateway API v1 CRDs (`gateway.networking.k8s.io/v1`) installed in the cluster.

## Security

The chart runs with a hardened security context by default:

- Non-root user (uid 101, nginx)
- Read-only root filesystem
- All capabilities dropped
- Seccomp RuntimeDefault profile
- emptyDir volumes for nginx writable paths (`/tmp`, `/var/cache/nginx`, `/var/run`)

{{ template "chart.valuesSection" . }}

{{ template "chart.maintainersSection" . }}
