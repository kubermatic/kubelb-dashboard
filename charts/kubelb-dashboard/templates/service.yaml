apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubelb-dashboard.fullname" . }}
  labels:
    {{- include "kubelb-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubelb-dashboard.selectorLabels" . | nindent 4 }}
